# Backlog

Unscheduled ideas — not committed work, listed so they aren't forgotten.

- Endless mode after clearing the 240 m run.
- Tighten the fallback palette's `terracotta` (`#C65B41`) out of the player
  pixel-scan tolerance box (±40 around `#D95763`) so the verify skill's scan
  cannot false-positive on fallback obstacles.
- Manifest-driven sheet pack script: per-frame source images under an
  `assets-src/` convention, composed into the packed sheets by a script that
  reads `SPRITE_SHEETS` / `TILE_SHEETS` and fails on size mismatches (see
  `docs/asset-workflow.md`).
